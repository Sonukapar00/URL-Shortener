# Configuration

This `config/` folder contains configuration guidance for deploying the project.

- `server/config/db.js` contains database connection logic.
- Use `.env.example` to create your own `.env` file with secure values.
