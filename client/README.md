# Client

This folder is reserved for frontend improvements and future React integration.

A dedicated client folder helps separate the UI layer from the backend API and improves the project structure for recruiters.
